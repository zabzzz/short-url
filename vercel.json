{
    "buildCommand": "cd short && npm install && npm run dev",
    "outputDirectory": "short/dist",
    "rewrites": [
        { "source": "/api/:path*", "destination": "/api/index.js" },
        { "source": "/(.*)", "destination": "/index.html" }
    ]
}