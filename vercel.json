{
    "buildCommand": "cd short && npm install && npm run build",
    "outputDirectory": "short/dist",
    "rewrites": [
        { "source": "/api/:path*", "destination": "/api/index.js" },
        { "source": "/(.*)", "destination": "/index.html" }
    ]
}